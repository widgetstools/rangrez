# npm workspaces. Peer deps are installed automatically since npm 7.
# `overrides` in the root package.json is what guarantees a single React and a
# single AG-Grid across the whole tree — see architecture §2.1.
save-exact=false
